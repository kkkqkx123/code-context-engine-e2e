# tests/e2e

这里是工作流测试的内部支持树，不是新的测试分层入口。

## 作用

- 承载工作流测试共用的模块。
- 提供 `helper/` 里的测试支撑能力。
- 保持旧工作流测试模块的组织方式，便于按场景继续拆分。

## 约束

- 不要在这里新增独立的回归测试文件。
- 不要把 benchmark 类测试放到这里。
- 不要把 fixture 输入数据放到这里，测试输入应使用 `tests/fixtures/`。

